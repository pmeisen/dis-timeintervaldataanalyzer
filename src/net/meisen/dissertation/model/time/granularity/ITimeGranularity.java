package net.meisen.dissertation.model.time.granularity;

/**
 * A {@code granularity} which can be used to create a {@code IntervalModel}.
 * The granularity defines the smallest unit which can be addressed by an
 * interval (the so called granule).
 * 
 * @author pmeisen
 * 
 */
public interface ITimeGranularity {
	// Nothing more
}
