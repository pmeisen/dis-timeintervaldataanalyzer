package net.meisen.dissertation.model.auth;

/**
 * A authentication-manager can be configured using the xml-configuration. If so
 * it can get it's configuration based on this interface.
 * 
 * @author pmeisen
 * 
 */
public interface IAuthManagerConfig {
	// nothing specified
}
