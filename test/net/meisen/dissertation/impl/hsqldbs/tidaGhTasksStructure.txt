SELECT
  DIM_DEG_TASKOID
, DIM_KEY_LOCATION
, DIM_KEY_PERSON
, DIM_KEY_TASKTYPE
, INTERVAL_END
, INTERVAL_START
, "VALUE"
FROM TB_INTERVALS

SELECT
  DURATION
, KEY_TASKTYPE
, OVERNIGHT
, TYPE
FROM TB_DIM_TASKTYPE

SELECT
  FISRTNAME
, KEY_PERSON
, LASTNAME
, PERSONNR
FROM TB_DIM_PERSON

SELECT
  DEPARTMENT
, KEY_LOCATION
, WORKAREA
FROM TB_DIM_LOCATION